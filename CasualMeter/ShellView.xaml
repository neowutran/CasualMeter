<controls:CasualMeterWindow x:Class="CasualMeter.ShellView"
                            xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                            xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                            xmlns:tb="http://www.hardcodet.net/taskbar"
                            xmlns:controls="clr-namespace:CasualMeter.Common.UI.Controls;assembly=CasualMeter.Common"
                            Title="ShellView" 
                            Height="300" 
                            Width="300"
                            Topmost="True"
                            ShowInTaskbar="False"
                            ResizeMode="NoResize"
                            WindowStyle="None"
                            AllowsTransparency="True"
                            Background="{x:Null}">
    <Grid>
        <tb:TaskbarIcon IconSource="Resources/CasualMeter.ico"
                        ToolTipText="Casual Meter">
            <tb:TaskbarIcon.ContextMenu>
                <ContextMenu>
                    <MenuItem Header="Exit"></MenuItem>
                </ContextMenu>
            </tb:TaskbarIcon.ContextMenu>
        </tb:TaskbarIcon>
        <StackPanel>
            <Rectangle Fill="Aqua" Width="50" Height="50" MouseDown="MoveControl"></Rectangle>
            <TextBox Width="30"></TextBox>
            <Button Width="40" Height="50"></Button>
        </StackPanel>
    </Grid>
</controls:CasualMeterWindow>
