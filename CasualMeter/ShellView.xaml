<lwpf:ClickThroughWindow x:Class="CasualMeter.ShellView"
                         xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                         xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                         xmlns:tb="http://www.hardcodet.net/taskbar"
                         xmlns:lwpf="clr-namespace:Lunyx.Common.UI.Wpf.Controls;assembly=Lunyx.Common.UI.Wpf"
                         Title="ShellView" 
                         Height="300" 
                         Width="300"
                         Topmost="True"
                         ShowInTaskbar="False"
                         ResizeMode="NoResize"
                         WindowStyle="None"
                         AllowsTransparency="True"
                         Background="{x:Null}">
    <Grid>
        <tb:TaskbarIcon x:Name="tbi_CasualMeter"
                        IconSource="Resources/CasualMeter.ico"
                        ToolTipText="Casual Meter">
            <tb:TaskbarIcon.ContextMenu>
                <ContextMenu>
                    <MenuItem Header="Exit"></MenuItem>
                </ContextMenu>
            </tb:TaskbarIcon.ContextMenu>
        </tb:TaskbarIcon>
        <StackPanel>
            <Rectangle Fill="Aqua" Width="50" Height="50" MouseDown="UIElement_OnMouseDown"></Rectangle>
            <TextBox Width="30"></TextBox>
            <Button Width="40" Height="50"></Button>
        </StackPanel>
            
        
    </Grid>
</lwpf:ClickThroughWindow>
